import React from 'react';
import { connect } from 'react-redux';
import PhotoBar from './photo_bar';


const mapSTP = (state, ownProps) => ({
    
})

const mapDTP = dispatch => ({
    
})

export default connect(mapSTP, null)(PhotoBar);